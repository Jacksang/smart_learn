# D3.2.1 — CSV/Excel Support (Function-Level Breakdown)

## Purpose
Implement CSV and Excel file ingestion for Smart Learn.

### Status Legend
- `[ ]` not started
- `[*]` in progress  
- `[x]` complete
- `[~]` intentionally bypassed / replaced

---

## D3.2.1.1 — Add CSV parsing dependency
- [ ] Add `papaparse` to `backend/package.json` (version `^5.4.0`)
- [ ] Commit with message: "D3.2.1.1: Add papaparse CSV parsing dependency"

**Scope:** `backend/package.json`
**Expected Result:** Dependencies updated, committed to git

---

## D3.2.1.2 — Add Excel parsing dependency
- [ ] Add `exceljs` to `backend/package.json` (version `^4.4.0`)
- [ ] Commit with message: "D3.2.1.2: Add exceljs Excel parsing dependency"

**Scope:** `backend/package.json`
**Expected Result:** Dependencies updated, committed to git

---

## D3.2.1.3 — Create CSV parser service
- [ ] Create `backend/src/ingestion/csv.js` with `parse(filePath)` function
- [ ] Return `{ text, metadata }` format
- [ ] Write unit tests for basic CSV, multi-line cells, quoted values

**Scope:** `backend/src/ingestion/csv.js`, `backend/src/ingestion/csv.test.js`
**Expected Result:** CSV parsing service with test coverage

---

## D3.2.1.4 — Create Excel parser service
- [ ] Create `backend/src/ingestion/excel.js` with `parse(filePath)` function
- [ ] Extract all sheets with metadata
- [ ] Return `{ text, metadata }` format
- [ ] Write unit tests for single/multi-sheet workbooks

**Scope:** `backend/src/ingestion/excel.js`, `backend/src/ingestion/excel.test.js`
**Expected Result:** Excel parsing service with test coverage

---

## D3.2.1.5 — Update ingestion service for CSV/Excel
- [ ] Extend `backend/src/ingestion/service.js` to route CSV/Excel files
- [ ] Add MIME type detection for `text/csv`, `application/vnd.ms-excel`, `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
- [ ] Write integration tests with sample files

**Scope:** `backend/src/ingestion/service.js`, `backend/src/ingestion/service.test.js`
**Expected Result:** CSV/Excel routing integrated into ingestion pipeline

---

## D3.2.1.6 — Update schema for CSV/Excel metadata
- [ ] Create migration `backend/db/schema/002_csv_excel_metadata.sql`
- [ ] Add `content_metadata` JSONB column to `source_materials`
- [ ] Write migration verification tests

**Scope:** `backend/db/schema/002_csv_excel_metadata.sql`
**Expected Result:** Schema migration added

---

## D3.2.1.7 — Add controller tests for CSV/Excel upload
- [ ] Add test coverage in `backend/src/ingestion/controller.test.js`
- [ ] Test successful CSV/Excel upload paths
- [ ] Test error handling for invalid files

**Scope:** `backend/src/ingestion/controller.test.js`
**Expected Result:** Test coverage for CSV/Excel upload

---

## D3.2.1.8 — Final commit and push
- [ ] Commit all CSV/Excel changes
- [ ] Push to `origin/main`

**Expected Result:** All code pushed to GitHub

---

## Worker Assignment
- Assign one slice per worker (fresh worker per slice)
- Timeout: 5 minutes per coding slice
- Main session handles git push after each verification

## Current Status
- Design: ✅ Complete (`docs/D3.2_FUTURE_INGESTION_FORMATS_DESIGN.md`)
- Breakdown: ✅ Complete (this file)
- D3.2.1.1: [*] In progress (worker spawned)
- Next: Wait for worker completion, then continue D3.2.1.2
