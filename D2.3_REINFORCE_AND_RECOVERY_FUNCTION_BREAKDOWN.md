# D2.3 Function-Level Breakdown — Reinforce and Recovery Flow

## Purpose
Break D2.3 into small coding tasks that are specific enough for a worker to execute like a bounded programming problem.

Parent item:
- `D2.3 — Reinforce and recovery flow`

Current known state:
- D2.1 learning sessions are implemented.
- D2.2 deferred questions / parking lot is implemented.
- Product rules already require encouragement-first tutoring, confidence recovery after difficulty, and easier-question fallback paths.
- D2.3 should connect struggle detection, recovery recommendations, and session-aware reinforce flow in an MVP-safe way.

## Status legend
- `[ ]` not started
- `[*]` in progress
- `[x]` complete
- `[~]` intentionally bypassed / replaced

---

## D2.3.1 — Audit existing schema/spec/docs for reinforce and recovery needs
- [ ] Verify current schema, endpoint spec, and motivation/recovery docs relevant to D2.3
- [ ] Confirm whether any reinforce/recovery backend surface already exists
- [ ] Define the minimum MVP contract for struggle detection and recovery actions

### Business logic
Before implementing reinforce/recovery logic, the system needs one clear MVP contract for when a learner is struggling and what recovery action the tutor should take.

### Exact coding problem
Audit current docs/code so later slices can implement reinforce/recovery logic without guessing about thresholds, outputs, or interaction points.

### Required touch points
- `docs/LESSON_STATE_MACHINE.md`
- `docs/FEEDBACK_TEMPLATE_CATALOG.md`
- `docs/API_ENDPOINT_SPEC.md`
- relevant backend files only if reinforce/recovery code already exists
- `TODO_DELIVERABLES_AND_CHECKPOINTS.md` only if assumptions need correction

### Expected result
- one short note/checklist update clarifies the D2.3 MVP contract

### Expected test outcomes
- no new runtime tests required in this audit slice unless an existing reinforce/recovery scaffold is already broken

---

## D2.3.2 — Add service logic for struggle detection and recovery recommendation
- [ ] Add service logic to detect struggle/confidence-drop signals from recent attempts/session state
- [ ] Add service logic to choose a basic recovery action
- [ ] Add focused service tests

### Business logic
When a learner struggles repeatedly, the tutor should recognize it and recommend a supportive next step instead of just continuing blindly.

### Exact coding problem
Implement a service that evaluates recent learning state and outputs a deterministic MVP recovery recommendation.

### Required touch points
- `backend/src/reinforce/service.js` or similar new module
- matching service tests

### Expected result
- one explicit service exists for struggle detection and recovery recommendation

### Expected test outcomes
- repeated low performance triggers recovery recommendation
- normal performance does not trigger false recovery
- edge cases are handled deterministically

---

## D2.3.3 — Add easier-question fallback selection logic
- [ ] Add service/helper logic to choose an easier fallback question path
- [ ] Add focused tests

### Business logic
If the learner is struggling, the tutor should be able to step down difficulty instead of repeating the same level of challenge.

### Exact coding problem
Implement the MVP easier-question fallback selection logic using available question/progress/session context.

### Required touch points
- reinforce/recovery service/module
- matching tests

### Expected result
- easier-question fallback recommendation logic exists and is test-backed

### Expected test outcomes
- fallback is suggested when struggle thresholds are met
- no fallback is suggested when not needed

---

## D2.3.4 — Add controller action for reinforce/recovery recommendation
- [ ] Add controller action to expose reinforce/recovery recommendation for a project/session
- [ ] Validate inputs
- [ ] Add focused controller tests

### Business logic
The frontend or tutor loop should be able to request the current reinforce/recovery recommendation for a learner in session context.

### Exact coding problem
Implement one controller action that returns the current recovery recommendation payload.

### Required touch points
- `backend/src/reinforce/controller.js` or similar
- matching controller tests

### Expected result
- one API-facing recommendation endpoint exists and is test-backed

### Expected test outcomes
- success case returns recovery recommendation
- invalid input is rejected
- unauthorized access is blocked

---

## D2.3.5 — Add end-of-session recovery summary structure
- [ ] Add service/controller support for a simple end-of-session recovery summary
- [ ] Add focused tests

### Business logic
At the end of a session, the learner should receive a constructive summary that reflects struggle/recovery state and suggests what to do next.

### Exact coding problem
Implement a minimal end-of-session recovery summary structure for MVP.

### Required touch points
- reinforce/recovery service/controller module
- matching tests

### Expected result
- a simple summary payload exists for session closeout/review

### Expected test outcomes
- summary includes recovery-oriented next-step guidance when relevant
- neutral summary is returned when no recovery signal exists

---

## D2.3.6 — Wire routes/module and close D2.3
- [ ] Wire reinforce/recovery routes/module
- [ ] Confirm recommendation/summary endpoints are reachable
- [ ] Update parent checklist accurately for D2.3 completion
- [ ] Create final D2.3 closeout commit

### Business logic
Once reinforce/recovery logic exists, it must be reachable through the API so later tutor orchestration can use it reliably.

### Exact coding problem
Wire the D2.3 routes and close the checklist item.

### Required touch points
- reinforce/recovery router/module
- server mount only if needed
- `TODO_DELIVERABLES_AND_CHECKPOINTS.md`

### Expected result
- D2.3 routes are implemented and reachable
- parent checklist is synced to repository reality

### Expected test outcomes
- focused D2.3 tests pass together
- route/controller wiring is proven directly or indirectly

---

## Recommended worker order
- [ ] D2.3.1 — audit existing schema/spec/docs for reinforce and recovery needs
- [ ] D2.3.2 — service logic for struggle detection and recovery recommendation
- [ ] D2.3.3 — easier-question fallback selection logic
- [ ] D2.3.4 — controller action for reinforce/recovery recommendation
- [ ] D2.3.5 — end-of-session recovery summary structure
- [ ] D2.3.6 — route wiring + closeout
