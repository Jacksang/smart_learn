# D2.2 Function-Level Breakdown — Deferred Questions / Parking Lot

## Purpose
Break D2.2 into small coding tasks that are specific enough for a worker to execute like a bounded programming problem.

Parent item:
- `D2.2 — Deferred questions / parking lot`

Current known state:
- D2.1 is complete and project-scoped learning sessions now exist.
- Product rules already say off-track or overly deep questions should be recorded for later and the student should be gently guided back unless they explicitly switch topics.
- D2.2 should add persisted deferred-question storage plus a basic revisit flow.

## Status legend
- `[ ]` not started
- `[*]` in progress
- `[x]` complete
- `[~]` intentionally bypassed / replaced

---

## D2.2.1 — Audit deferred-question schema/spec/current code surface
- [ ] Verify schema and API expectations for deferred questions
- [ ] Confirm whether any deferred-question backend code already exists
- [ ] Define the minimum MVP contract before implementation starts

### Business logic
The tutor needs a safe place to park off-track questions so the session can stay focused without losing the learner’s curiosity.

### Exact coding problem
Audit current schema/docs/code so later slices can implement deferred-question storage and revisit flow without guessing about fields, ownership rules, or route shapes.

### Required touch points
- `backend/db/schema/001_baseline.sql`
- `docs/API_ENDPOINT_SPEC.md`
- `docs/LESSON_STATE_MACHINE.md`
- any deferred-question files only if they already exist
- `TODO_DELIVERABLES_AND_CHECKPOINTS.md` only if assumptions need correction

### Expected result
- one short note/checklist update clarifies the MVP deferred-question contract

### Expected test outcomes
- no new runtime tests required in this audit slice unless existing deferred-question scaffolding is already broken

---

## D2.2.2 — Add repository helpers for project-scoped deferred questions
- [ ] Add repository helpers to create/list/update deferred questions for an owned project/session
- [ ] Enforce ownership and stable ordering
- [ ] Add focused repository tests

### Business logic
A learner’s parked questions should be persisted under the correct project/session so they can be revisited later without leaking across projects.

### Exact coding problem
Implement repository helpers for creating deferred questions, listing them, and marking revisit status in a project-scoped way.

### Required touch points
- `backend/src/deferred-questions/repository.js`
- `backend/src/deferred-questions/repository.test.js`

### Expected result
- one ownership-safe persistence layer exists for deferred questions

### Expected test outcomes
- owned project/session create/list/update works
- cross-project leakage is blocked
- empty state is handled cleanly

---

## D2.2.3 — Add service logic for defer/revisit state handling
- [ ] Add helper/service logic for deferred-question state transitions
- [ ] Support basic statuses such as parked / revisited / resolved if appropriate for MVP
- [ ] Add focused service tests

### Business logic
Deferred questions need a small, explicit state model so the tutor can tell whether a question is merely parked, has been revisited, or has been resolved.

### Exact coding problem
Implement a small service layer that normalizes deferred-question state and revisit behavior for MVP.

### Required touch points
- `backend/src/deferred-questions/service.js`
- `backend/src/deferred-questions/service.test.js`

### Expected result
- deferred-question state logic is explicit and testable outside controllers

### Expected test outcomes
- supported statuses/updates are normalized correctly
- invalid transitions or values are rejected cleanly if needed

---

## D2.2.4 — Add controller action to park a deferred question
- [ ] Add project/session-scoped controller action to create a deferred question
- [ ] Validate input fields
- [ ] Add focused controller tests

### Business logic
When the learner asks an off-track question, the tutor should be able to park it for later without disrupting the current lesson.

### Exact coding problem
Implement one controller action to create a deferred question record tied to the current project/session/topic context.

### Required touch points
- `backend/src/deferred-questions/controller.js`
- `backend/src/deferred-questions/controller.test.js`

### Expected result
- deferred-question create endpoint exists and is test-backed

### Expected test outcomes
- success case creates deferred question payload
- invalid input is rejected
- unauthorized/not-found project/session is rejected cleanly

---

## D2.2.5 — Add controller action to list deferred questions
- [ ] Add controller action to list deferred questions for a project/session
- [ ] Add focused controller tests

### Business logic
The tutor or frontend should be able to fetch the learner’s parked questions so they can be revisited intentionally later.

### Exact coding problem
Implement one controller action that returns deferred questions in a stable useful order.

### Required touch points
- `backend/src/deferred-questions/controller.js`
- `backend/src/deferred-questions/controller.test.js`

### Expected result
- deferred-question listing endpoint exists and is test-backed

### Expected test outcomes
- success case returns deferred-question list
- empty state is handled cleanly
- unauthorized access is blocked

---

## D2.2.6 — Add controller action to mark revisit/resolution state
- [ ] Add controller action to update deferred-question revisit/resolution state
- [ ] Add focused controller tests

### Business logic
Once the tutor comes back to a parked question, the system should be able to mark that question as revisited or resolved.

### Exact coding problem
Implement a controller action to update the state of one deferred question.

### Required touch points
- `backend/src/deferred-questions/controller.js`
- `backend/src/deferred-questions/controller.test.js`

### Expected result
- deferred-question state update endpoint exists and is test-backed

### Expected test outcomes
- success case updates the question state
- invalid identifiers/status values are rejected
- unauthorized access is blocked

---

## D2.2.7 — Wire routes/module and close D2.2
- [ ] Wire deferred-question routes/module
- [ ] Confirm create/list/update endpoints are reachable
- [ ] Update parent checklist accurately for D2.2 completion
- [ ] Create final D2.2 closeout commit

### Business logic
Once deferred-question create/list/update flows exist, they must be reachable through the project-scoped API so later tutor orchestration can use them.

### Exact coding problem
Wire the D2.2 routes and close the checklist item.

### Required touch points
- deferred-question router/module
- server mount only if needed
- `TODO_DELIVERABLES_AND_CHECKPOINTS.md`

### Expected result
- D2.2 routes are implemented and reachable
- parent checklist is synced to repository reality

### Expected test outcomes
- focused deferred-question tests pass together
- route/controller wiring is proven directly or indirectly

---

## Recommended worker order
- [ ] D2.2.1 — audit deferred-question schema/spec/current code surface
- [ ] D2.2.2 — repository helpers for project-scoped deferred questions
- [ ] D2.2.3 — service logic for defer/revisit state handling
- [ ] D2.2.4 — controller action to park a deferred question
- [ ] D2.2.5 — controller action to list deferred questions
- [ ] D2.2.6 — controller action to mark revisit/resolution state
- [ ] D2.2.7 — route wiring + closeout
