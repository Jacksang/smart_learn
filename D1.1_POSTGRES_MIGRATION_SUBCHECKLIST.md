# D1.1 PostgreSQL Migration Foundation — Sub-Checklist

## Purpose
This file breaks D1.1 into smaller, precise implementation tasks so coding workers can execute with tighter scope and clearer boundaries.

Parent deliverable:
- `D1.1 PostgreSQL migration foundation` in `TODO_DELIVERABLES_AND_CHECKPOINTS.md`

Rules:
- Work items here should be executed one checkpoint at a time.
- Each worker run should target only the first unfinished `[ ]` item.
- Do not jump to D1.2+ while working this file.
- If the same step fails twice, stop and analyze before proceeding.

---

## D1.1.A — Persistence strategy decision
- [x] Inspect current backend dependencies and Mongo-oriented runtime paths
  - Findings (2026-04-04): root `package.json` still depends on `mongoose`; active backend entrypoint is `backend/server.js` -> `backend/config/server.js` -> `backend/config/database.js`.
  - `backend/config/database.js` is already PostgreSQL-oriented (`pg` `Pool` + `SELECT 1` health probe), but root `package.json` does not yet declare `pg`.
  - Active runtime controllers still load Mongoose models: `backend/src/users/{controller,middleware}.js`, `backend/src/outline/controller.js`, `backend/src/questions/controller.js`, `backend/src/answers/controller.js`.
  - Mongo-only model layer remains under `backend/models/{User,Outline,Question,Answer}.js`.
- [ ] Decide PostgreSQL access approach for MVP (`pg` vs ORM/query builder)
- [ ] Record decision, rationale, and migration scope
- [ ] Save artifact: `docs/POSTGRES_MIGRATION_PLAN.md`
- [ ] Commit and push

## D1.1.B — Runtime retirement plan for Mongo path
- [ ] Inventory files importing `mongoose` or Mongo-style models
- [ ] Define which files must be retired, replaced, or temporarily shimmed
- [ ] Record exact runtime path changes needed to remove Mongo from active path
- [ ] Update artifact: `docs/POSTGRES_MIGRATION_PLAN.md`
- [ ] Commit and push

## D1.1.C — PostgreSQL bootstrap design
- [ ] Define environment variables and connection config structure
- [ ] Define schema bootstrap / migration approach
- [ ] Define folder/file structure for SQL or migration files
- [ ] Update artifact: `docs/POSTGRES_MIGRATION_PLAN.md`
- [ ] Commit and push

## D1.1.D — Dependency and config changes
- [ ] Add chosen PostgreSQL package(s)
- [ ] Remove or stop using Mongo-only runtime dependencies where safe
- [ ] Add PostgreSQL config module scaffold
- [ ] Save artifact: dependency/config changes committed
- [ ] Commit and push

## D1.1.E — Initial schema bootstrap
- [ ] Create initial PostgreSQL schema/bootstrap file for MVP core tables
- [ ] Ensure schema aligns with `docs/POSTGRESQL_SCHEMA_SPEC.md`
- [ ] Add setup instructions
- [ ] Save artifact: bootstrap/migration files + `docs/POSTGRES_SETUP.md`
- [ ] Commit and push

## D1.1.F — Retire Mongo from active runtime path
- [ ] Remove/replace active runtime imports that depend on Mongoose models
- [ ] Ensure server startup path no longer requires Mongo
- [ ] Keep behavior explicitly MVP-safe even if some endpoints remain placeholder-based
- [ ] Save artifact: working runtime path + notes
- [ ] Commit and push

## D1.1.G — Close parent checkpoint
- [ ] Sync completed D1.1 work back into `TODO_DELIVERABLES_AND_CHECKPOINTS.md`
- [ ] Mark D1.1 parent checkpoints done as appropriate
- [ ] Commit and push

---

## Current recommended next checkpoint
- [ ] Complete D1.1.A and push `docs/POSTGRES_MIGRATION_PLAN.md`
