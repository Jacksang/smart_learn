# Smart Learn Outline Generation Flow - Sub-Checklist

## Purpose
Track implementation progress for outline generation flow, including outline creation, refresh on material change, and the `outlines` + `outline_items` tables.

## Status: Started (2026-04-04)

---

## D1.3.A — Create `outlines` and `outline_items` tables

### Deliverables
- [x] Create `backend/db/schema/002_outline_tables.sql` with:
  - Added `backend/db/schema/002_outline_tables.sql` using the numbered PostgreSQL migration pattern (`BEGIN`/`COMMIT`).
  - Added `outlines` table with `id`, `project_id`, `title`, `status`, `created_at`, `updated_at`.
  - Added `outline_items` table with `id`, `outline_id`, `parent_item_id`, `level`, `title`, `content`, `order_index`, `created_at`.
  - Added required foreign keys and indexes for outline ownership and item ordering.
  - Moved outline-related definitions out of `backend/db/schema/001_baseline.sql` so `001` can bootstrap before `002` attaches outline tables and dependent foreign keys.
- [x] Verify schema migration runs successfully
  - Static verification completed by checking numbered migration ordering and removing pre-`002` references to `outlines` / `outline_items` from `001_baseline.sql` table definitions.
  - Final runtime verification (2026-04-04): sourced the repo-local `.env`, confirmed it only overrides `DB_PASSWORD`, and verified that the active local credential path for this machine is `openclaw_user@127.0.0.1:5432/openclaw_db` rather than the repo default `smartlearn@localhost:5432/smartlearn`.
  - Applied `backend/db/schema/001_baseline.sql` and `backend/db/schema/002_outline_tables.sql` successfully inside a disposable schema on `openclaw_db` by prefixing each migration with `SET search_path TO <temp_schema>, public;`, which avoided mutating the real application tables while still exercising the committed SQL exactly.
  - Verified the migrated disposable schema contained `outlines`, `outline_items`, the downstream foreign keys from `learning_projects`, `questions`, `progress_snapshots`, `learning_sessions`, and `deferred_questions`, plus the `idx_outline_items_outline_id_order_index` index and `outlines_set_updated_at` trigger.
- [x] Save artifact: schema file + migration verification
  - Artifact files: `backend/db/schema/001_baseline.sql`, `backend/db/schema/002_outline_tables.sql`
  - Verification artifacts: env-backed config probe via `node` + `backend/config/postgres.js`, authenticated `psql` connectivity check against `openclaw_user@openclaw_db`, and disposable-schema replay of `001_baseline.sql` + `002_outline_tables.sql` with schema/object assertions afterward.
- [x] Commit and push
  - Scoped local commit created for D1.3.A after runtime migration verification passed.

---

## D1.3.B — Implement outline creation endpoint

### Deliverables
- [x] Create `backend/src/outline/service.js` with `createOutline()` method
  - Added service-level `createOutline()` orchestration with payload validation, normalization, and nested item flattening for insert-ready outline items.
- [x] Create `backend/src/outline/controller.js` with `POST /api/outline` endpoint
  - Implemented controller normalization for camelCase/snake_case request bodies and `POST /api/outline` response handling.
- [x] Create `backend/src/outline/router.js` (already exists, verify route wiring)
  - Verified existing router wiring still exposes `router.post('/', protect, createOutline)`.
- [x] Implement request validation for outline creation payload
  - Added validation for `projectId`, `title`, `status`, and nested `items`/`children` fields including `level`, `orderIndex`, and `content` constraints.
- [x] Save artifact: service + controller + route files + tests
  - Artifacts: `backend/src/outline/service.js`, `backend/src/outline/controller.js`, `backend/src/outline/repository.js`, `backend/src/outline/service.test.js`, `backend/src/outline/controller.test.js`
  - Verification: `npx jest backend/src/outline/service.test.js backend/src/outline/controller.test.js --runInBand`
- [ ] Commit and push

---

## D1.3.C — Implement outline refresh on material change

### Deliverables
- [ ] Create `backend/src/outline/service.js` with `refreshOutline()` method
- [ ] Add event listener/hook for `source_materials` changes
- [ ] Implement outline regeneration logic when materials are updated/added
- [ ] Save artifact: service updates + event handling code + tests
- [ ] Commit and push

---

## D1.3.D — Implement outline retrieval endpoints

### Deliverables
- [ ] Create `backend/src/outline/controller.js` with `GET /api/outline/:id` endpoint
- [ ] Implement outline retrieval with nested `outline_items`
- [ ] Implement `GET /api/outline/project/:projectId` for project-specific outlines
- [ ] Save artifact: controller updates + tests
- [ ] Commit and push

---

## D1.3.E — Test outline generation flow end-to-end

### Deliverables
- [ ] Create integration tests for outline creation flow
- [ ] Test outline refresh triggered by material changes
- [ ] Test nested outline structure retrieval
- [ ] Save artifact: test files + test results
- [ ] Commit and push

---

## D1.3.F — Document outline generation flow

### Deliverables
- [ ] Create `docs/OUTLINE_GENERATION_FLOW.md` with:
  - Outline table schema explanation
  - Outline refresh trigger flow diagram
  - API endpoint documentation
  - Usage examples
- [ ] Save artifact: documentation file
- [ ] Commit and push

---

## Current recommended next checkpoint
- [ ] Start D1.3.B — Implement outline creation endpoint

---

## Work Protocol
- Before each work block, define the exact deliverable ID from this file.
- Do not report "almost done" unless a file, commit, or test artifact exists.
- If no artifact is produced within the intended work window, record the blocker.
- Prefer milestone completion over broad status updates.
