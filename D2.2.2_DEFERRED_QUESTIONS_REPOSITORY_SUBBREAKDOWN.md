# D2.2.2 Sub-Breakdown — Deferred Questions Repository

## Purpose
Break D2.2.2 into smaller repository-only slices because the original repository task timed out twice before producing usable artifacts.

Parent item:
- `D2.2.2 — Add repository helpers for project-scoped deferred questions`

## Status legend
- `[ ]` not started
- `[*]` in progress
- `[x]` complete
- `[~]` intentionally bypassed / replaced

---

## D2.2.2a — Create repository scaffold and mapping helpers
- [x] Create `backend/src/deferred-questions/repository.js`
- [x] Add basic row-mapping helper(s) only
- [x] Add minimal repository test scaffold if needed

### Business logic
Before adding query logic, the deferred-question repository needs a stable file structure and row mapping approach consistent with the rest of the backend.

### Exact coding problem
Create the repository file and only the minimum internal scaffolding needed for later query helpers.

### Allowed scope
- `backend/src/deferred-questions/repository.js`
- `backend/src/deferred-questions/repository.test.js`

### Expected result
- repository module exists
- mapping/helper structure is in place
- no broad query logic yet

### Expected test outcomes
- test scaffold runs or at least imports cleanly if a first minimal test is added

---

## D2.2.2b — Add create helper for owned deferred questions
- [x] Add repository helper to create one deferred question for an owned project/session
- [x] Add focused repository test for create flow

### Business logic
The tutor must be able to park one question into the project/session parking lot.

### Exact coding problem
Implement only the create helper with ownership-aware project/session validation assumptions matching existing backend patterns.

### Allowed scope
- `backend/src/deferred-questions/repository.js`
- `backend/src/deferred-questions/repository.test.js`

### Expected result
- one create helper exists for deferred questions

### Expected test outcomes
- successful create path works
- returned row is mapped consistently

---

## D2.2.2c — Add list helper with stable ordering
- [x] Add repository helper to list deferred questions for an owned project/session
- [x] Add focused repository test for list ordering and empty state

### Business logic
The tutor/frontend must be able to retrieve parked questions in a stable order for revisit.

### Exact coding problem
Implement only the list helper with stable ordering and clean empty-state behavior.

### Allowed scope
- `backend/src/deferred-questions/repository.js`
- `backend/src/deferred-questions/repository.test.js`

### Expected result
- one list helper exists with stable ordering

### Expected test outcomes
- ordered list is returned correctly
- empty list state is handled cleanly

---

## D2.2.2d — Add update-status helper and close D2.2.2
- [ ] Add repository helper to update deferred-question revisit/resolution state
- [ ] Add focused repository test for update flow
- [ ] Update parent D2.2.2 breakdown/checklist state

### Business logic
Deferred questions must be markable as revisited or resolved once the tutor comes back to them.

### Exact coding problem
Implement only the status-update helper and close the repository slice cleanly.

### Allowed scope
- `backend/src/deferred-questions/repository.js`
- `backend/src/deferred-questions/repository.test.js`
- `D2.2_DEFERRED_QUESTIONS_FUNCTION_BREAKDOWN.md`
- `D2.2.2_DEFERRED_QUESTIONS_REPOSITORY_SUBBREAKDOWN.md`

### Expected result
- create/list/update repository coverage for D2.2.2 is complete

### Expected test outcomes
- update path works
- focused deferred-question repository tests pass together

---

## Recommended worker order
- [x] D2.2.2a — repository scaffold and mapping helpers
- [x] D2.2.2b — create helper
- [x] D2.2.2c — list helper
- [ ] D2.2.2d — update-status helper and closeout
