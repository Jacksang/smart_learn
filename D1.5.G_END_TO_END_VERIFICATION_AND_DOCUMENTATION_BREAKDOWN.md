# D1.5.G Function-Level Breakdown — End-to-End Verification and Documentation

## Purpose
Break D1.5.G into small coding/documentation tasks that are specific enough for a worker to execute like a bounded programming problem.

Parent item:
- `D1.5.G — Verify D1.5 end-to-end and document the flow`

Current known state:
- D1.5.A–F are implemented across answers, evaluation, progress refresh, and progress retrieval.
- D1.5.G should verify the end-to-end loop using those persisted APIs and then close out docs/checklists.
- This task is broad because it mixes integration testing, documentation, and checklist synchronization.

## Status legend
- `[ ]` not started
- `[*]` in progress
- `[x]` complete
- `[~]` intentionally bypassed / replaced

---

## D1.5.G1 — Audit current D1.5 coverage and define end-to-end verification contract
- [ ] Verify which D1.5 routes/services/tests already exist
- [ ] Define the exact end-to-end loop to prove
- [ ] Record the minimum documentation/closeout contract for D1.5.G

### Business logic
Before writing end-to-end tests, the implementation must define exactly which user journey D1.5 is proving: question answering, answer history, evaluation, progress refresh, and weak-area retrieval.

### Exact coding problem
Audit current D1.5 files/tests so later slices can add only the missing integration coverage and closeout docs without duplicating existing unit/controller tests.

### Required touch points
- `backend/src/answers/`
- `backend/src/progress/`
- existing D1.5 test files
- `D1.5_ANSWERS_AND_PROGRESS_SUBCHECKLIST.md`
- this breakdown file

### Expected result
- one short audit note/checklist update confirms what D1.5.G must prove end-to-end
- later slices know exact APIs and expected final artifacts

### Expected test outcomes
- no new runtime tests required in this slice unless audit reveals an already-broken D1.5 test surface

---

## D1.5.G2 — Add end-to-end integration coverage for answer submission and answer history
- [ ] Add integration test flow covering answer submission then question/project answer history retrieval
- [ ] Assert persisted attempt data is returned correctly
- [ ] Keep test scope limited to D1.5 loop inputs/outputs

### Business logic
A learner should be able to submit an answer and then see that attempt appear in both question-level and project-level answer history.

### Exact coding problem
Add an integration test that exercises:
- `POST /api/projects/:projectId/questions/:questionId/answers`
- `GET /api/projects/:projectId/questions/:questionId/answers`
- `GET /api/projects/:projectId/answers/history`

### Required touch points
- likely `backend/src/answers/flow.integration.test.js` or equivalent new D1.5 integration test file
- any shared test helpers needed for setup only

### Expected result
- one integration test proves persisted answer attempts flow through retrieval endpoints correctly

### Expected test outcomes
- submitted answer appears in question-level history
- submitted answer appears in project-level history
- returned attempt data includes expected correctness/score fields

---

## D1.5.G3 — Add end-to-end integration coverage for explicit evaluation
- [ ] Add integration test for explicit evaluation by `answerAttemptId`
- [ ] Assert evaluation reuses persisted attempt/question context

### Business logic
A learner or frontend should be able to explicitly re-evaluate a stored answer attempt and receive deterministic evaluation output without resubmitting the answer.

### Exact coding problem
Add an integration test that exercises:
- answer submission
- `POST /api/projects/:projectId/answers/evaluate`

### Required touch points
- D1.5 integration test file
- shared setup helpers only if needed

### Expected result
- explicit evaluation is proven end-to-end from persisted attempt data

### Expected test outcomes
- evaluation works using `answerAttemptId`
- returned evaluation matches the stored question/answer context deterministically
- invalid or missing attempt path remains covered by existing controller tests, not necessarily duplicated here

---

## D1.5.G4 — Add end-to-end integration coverage for progress refresh and weak-area retrieval
- [ ] Add integration test for progress refresh after answer activity
- [ ] Add integration test for weak-area retrieval from persisted snapshots
- [ ] Assert refresh persists project/topic snapshots and retrieval reads persisted data

### Business logic
Once answer attempts exist, the system should refresh project progress, persist snapshots, and allow the frontend to retrieve weak areas from stored snapshot data.

### Exact coding problem
Add an integration test that exercises:
- answer submission(s)
- `POST /api/projects/:projectId/progress/refresh`
- `GET /api/projects/:projectId/progress`
- `GET /api/projects/:projectId/progress/topics/:itemId`
- `GET /api/projects/:projectId/progress/weak-areas`

### Required touch points
- D1.5 integration test file
- progress/answer test helpers only if needed

### Expected result
- one integration flow proves snapshot persistence and retrieval work together

### Expected test outcomes
- refresh returns/persists project snapshot
- topic progress endpoint returns latest topic snapshot
- weak-areas endpoint returns persisted weak-area output

---

## D1.5.G5 — Add/update D1.5 flow documentation
- [ ] Create or update a D1.5 answers/progress flow doc
- [ ] Describe the verified end-to-end loop and persisted snapshot behavior
- [ ] Keep docs aligned with actual implemented endpoints

### Business logic
The verified D1.5 loop should be documented clearly enough that future work can build on it without rediscovering how answers, evaluation, progress refresh, and weak-area retrieval fit together.

### Exact coding problem
Produce or update a doc that explains:
- the D1.5 answer/evaluation/progress lifecycle
- the endpoints involved
- how persisted `answer_attempts` and `progress_snapshots` are used
- any MVP limitations

### Required touch points
- likely `docs/ANSWERS_AND_PROGRESS_FLOW.md` or similar
- checklist notes only if needed

### Expected result
- one concise doc exists and matches the implemented API behavior

### Expected test outcomes
- documentation alignment review only; no runtime tests required in this slice

---

## D1.5.G6 — Final D1.5 closeout and parent checklist sync
- [ ] Run the focused D1.5 test set
- [ ] Update D1.5 sub-checklist for final completion
- [ ] Update `TODO_DELIVERABLES_AND_CHECKPOINTS.md` for D1.5 completion if evidence is complete
- [ ] Create final D1.5.G closeout commit

### Business logic
D1.5 should close only when the whole answers-and-progress flow is implemented, test-backed, documented, and reflected correctly in the parent tracking files.

### Exact coding problem
Run the final focused verification set and sync the checklists so D1.5 can be treated as complete.

### Required touch points
- D1.5 integration test file(s)
- `D1.5_ANSWERS_AND_PROGRESS_SUBCHECKLIST.md`
- `TODO_DELIVERABLES_AND_CHECKPOINTS.md`
- D1.5 doc file if created in G5

### Expected result
- D1.5 is marked complete with exact evidence
- parent checklist no longer lags behind the sub-checklist

### Expected test outcomes
- focused D1.5 tests pass together
- end-to-end D1.5 loop is proven by integration coverage
- checklist state matches repository reality

---

## Recommended worker order
- [ ] D1.5.G1 — audit D1.5 coverage and define verification contract
- [ ] D1.5.G2 — integration coverage for answer submission/history
- [ ] D1.5.G3 — integration coverage for explicit evaluation
- [ ] D1.5.G4 — integration coverage for progress refresh and weak-area retrieval
- [ ] D1.5.G5 — documentation update
- [ ] D1.5.G6 — final closeout and parent checklist sync

## Notes for worker prompts
Each worker prompt should target only one of D1.5.G1–D1.5.G6 and explicitly list:
- the exact test/doc/checklist slice to implement
- the exact files allowed to change
- the exact expected evidence
- whether a local commit is required for that slice
