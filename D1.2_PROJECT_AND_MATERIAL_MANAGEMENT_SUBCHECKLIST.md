# D1.2 Project and Material Management — Sub-Checklist

## Purpose
This file breaks D1.2 into smaller, precise implementation tasks so coding workers can execute with tighter scope and clearer boundaries.

Parent deliverable:
- `D1.2 Project and material management` in `TODO_DELIVERABLES_AND_CHECKPOINTS.md`

Rules:
- Work items here should be executed one checkpoint at a time.
- Each worker run should target only the first unfinished `[ ]` item.
- Do not jump to D1.3+ while working this file.
- If the same step fails twice, stop and analyze before proceeding.

---

## D1.2.A — Runtime and schema alignment audit
- [x] Inspect current PostgreSQL schema, repositories, routes, and controllers related to projects/materials
  - Inspected schema: `backend/db/schema/001_baseline.sql`
  - Inspected runtime entrypoints: `backend/config/server.js`, `backend/config/database.js`, `backend/server.js`
  - Inspected existing repository/controller/router patterns: `backend/src/outline/*`, `backend/src/questions/*`, `backend/src/answers/*`
  - Initial finding from inspection only: schema already defines `learning_projects` and `source_materials`, but no matching `backend/src/projects/*` or `backend/src/materials/*` runtime modules/routes are present yet.
- [x] Identify missing runtime pieces for `learning_projects` and `source_materials`
  - Missing `backend/src/projects/` runtime module set analogous to existing features: at minimum `repository.js`, `controller.js`, and `router.js` for `learning_projects` CRUD/list flows.
  - Missing `backend/src/materials/` runtime module set: at minimum `repository.js`, `controller.js`, and `router.js` for `source_materials` CRUD/list flows bound to a project.
  - Missing route registration in `backend/config/server.js` for `/api/projects` and `/api/materials` (or equivalent project-scoped material endpoints).
  - Missing service-layer logic tying project records to material weighting/fallback behavior before outline/question generation paths consume them.
  - Missing tests covering PostgreSQL-backed project/material repository behavior and mounted routes; current runtime only exposes users, outline, questions, and answers.
- [x] Record required table/column/API alignment notes
  - `learning_projects` table contract for MVP runtime should expose: `id`, `user_id`, `title`, `description`, `subject`, `status`, `current_mode`, `current_outline_id`, `created_at`, `updated_at`.
  - `learning_projects.current_outline_id` is nullable and points to `outlines.id` with `ON DELETE SET NULL`; project read endpoints should therefore tolerate `currentOutlineId = null` and must not assume an outline already exists.
  - Project list/read queries should be scoped by authenticated `user_id`, matching the existing `protect` middleware pattern already used by outline/question/answer routes.
  - `source_materials` table contract for MVP runtime should expose: `id`, `project_id`, `source_kind`, `material_type`, `title`, `original_file_name`, `mime_type`, `storage_path`, `raw_text`, `extracted_text`, `weight`, `is_active`, `source_version`, `created_at`, `updated_at`.
  - Material writes must respect schema constraints directly: `project_id` required FK, `weight >= 0`, `source_version > 0`, `is_active` default `true`, and default `weight = 1.00` when callers omit weighting.
  - API shape should use project-scoped ownership checks even if materials are mounted at `/api/materials`: every create/read/update/list flow must verify the referenced project belongs to `req.user.id` before returning or mutating rows.
  - Recommended MVP routes from the current Express mounting style: `GET /api/projects`, `POST /api/projects`, `GET /api/projects/:projectId`, `PATCH /api/projects/:projectId`, `GET /api/projects/:projectId/materials`, `POST /api/projects/:projectId/materials`, and `PATCH /api/materials/:materialId` for status/weight/content updates.
  - Response/request field names should stay close to schema names (snake_case in SQL, camelCase mapping only at controller boundary if adopted consistently); avoid repeating the current outline runtime mismatch where `backend/src/outline/repository.js` still queries legacy columns such as `course_title`, `source_type`, `topics`, and `ai_summary` that do not exist in the committed PostgreSQL `outlines` schema.
  - Because `backend/config/server.js` currently mounts only `/api/users`, `/api/outline`, `/api/questions`, and `/api/answers`, adding project/material APIs requires explicit router registration there before any integration tests can pass.
- [x] Save artifact: audit notes in this file or a linked doc
  - Saved linked audit artifact: `docs/D1.2_RUNTIME_SCHEMA_ALIGNMENT_AUDIT.md`
- [x] Commit and push

## D1.2.B — `learning_projects` persistence layer
- [x] Implement repository/module support for `learning_projects`
- [x] Define create/read/update/list operations needed for MVP
  - Added MVP repository operations in `backend/src/projects/repository.js`: `listByUser`, `createProject`, `findByIdForUser`, and `updateProjectForUser`.
  - Update path restricts mutations to schema-aligned fields: `title`, `description`, `subject`, `status`, `current_mode`, and `current_outline_id`.
  - All read/update flows remain user-scoped via `(projectId, userId)` lookups to match authenticated ownership expectations.
- [x] Ensure PostgreSQL queries match the committed schema/bootstrap
  - Verified `backend/src/projects/repository.js` queries against `backend/db/schema/001_baseline.sql`: all selected/inserted/updated columns exist on `learning_projects` (`id`, `user_id`, `title`, `description`, `subject`, `status`, `current_mode`, `current_outline_id`, `created_at`, `updated_at`).
  - Confirmed create defaults remain schema-safe: repository falls back to `status = 'active'` while schema default is also `active`; nullable fields (`description`, `subject`, `current_mode`, `current_outline_id`) are inserted as `NULL` when omitted.
  - Confirmed update scope is bootstrap-aligned: mutable fields are restricted to real table columns only, and `updated_at` remains trigger-managed by `learning_projects_set_updated_at` from the baseline schema.
- [x] Save artifact: working repository code
  - Artifact confirmed in repo: `backend/src/projects/repository.js`
- [x] Commit and push

## D1.2.C — `source_materials` persistence layer
- [x] Implement repository/module support for `source_materials`
  - Added repository module artifact: `backend/src/materials/repository.js`
  - Repository exports schema-aligned material mapping/constants plus base persistence helpers: `listByProject`, `createMaterial`, `findById`, and `updateMaterial`.
- [x] Define create/read/update/list operations needed for MVP
  - MVP repository operations confirmed in `backend/src/materials/repository.js`: `listByProject(projectId)`, `createMaterial(payload)`, `findById(materialId)`, and `updateMaterial(materialId, updates)`.
  - Create contract is schema-aligned for MVP ingestion: requires `projectId`, `sourceKind`, and `materialType`; optionally accepts `title`, `originalFileName`, `mimeType`, `storagePath`, `rawText`, `extractedText`, `weight`, `isActive`, and `sourceVersion`.
  - Read/list contract currently supports project-scoped listing plus direct single-record lookup, which is sufficient for upcoming route/controller wiring without adding delete/version-history scope yet.
  - Update contract is intentionally limited to mutable schema-backed fields only: `source_kind`, `material_type`, `title`, `original_file_name`, `mime_type`, `storage_path`, `raw_text`, `extracted_text`, `weight`, `is_active`, and `source_version`.
  - MVP omission kept explicit: no delete/archive helper was added in this checkpoint because later D1.2 tasks focus on linkage, weighting, fallback behavior, and route exposure rather than destructive material lifecycle operations.
- [x] Ensure project-material linkage is enforced correctly
  - Added user-scoped repository helpers in `backend/src/materials/repository.js`: `listByProjectForUser`, `createMaterialForUser`, `findByIdForUser`, and `updateMaterialForUser`.
  - Ownership enforcement now happens in SQL by joining `learning_projects` on `project_id` and requiring `learning_projects.user_id = userId` before listing, creating, reading, or updating project materials.
  - This keeps material access aligned with the existing authenticated project ownership model before route/controller wiring is added in later D1.2 steps.
- [x] Save artifact: working repository code
  - Artifact confirmed in repo: `backend/src/materials/repository.js`
- [ ] Commit and push

## D1.2.D — Material weighting rules
- [ ] Implement material weighting fields/logic in the backend service layer
- [ ] Define MVP-safe defaults for weight/priority handling
- [ ] Cover edge cases for missing or low-priority material inputs
- [ ] Save artifact: service logic + tests
- [ ] Commit and push

## D1.2.E — Base knowledge fallback trigger
- [ ] Implement fallback trigger when project materials are insufficient
- [ ] Define decision point and response shape for fallback behavior
- [ ] Keep behavior explicit and testable for MVP
- [ ] Save artifact: service logic + tests
- [ ] Commit and push

## D1.2.F — Routes and integration tests
- [ ] Expose working routes for project/material management
- [ ] Add integration/unit tests covering core CRUD and fallback behavior
- [ ] Verify end-to-end route flow against PostgreSQL-backed persistence
- [ ] Save artifact: working routes + tests
- [ ] Commit and push

## D1.2.G — Close parent checkpoint
- [ ] Sync completed D1.2 work back into `TODO_DELIVERABLES_AND_CHECKPOINTS.md`
- [ ] Mark D1.2 parent checkpoints done as appropriate
- [ ] Commit and push

---

## Current recommended next checkpoint
- [ ] Complete D1.2.A and push the runtime/schema alignment audit
